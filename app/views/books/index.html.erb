<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="style.css">
    </head>
    <body>
        <h2>Books</h2>
        <table>
                <th>Title</th>
                <th>Body</th>
             <% @books.each do |book| %>
            <tr>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
                <td><%= link_to 'show',book_path(book.id) %> </td>
                <td><%= link_to 'edit',edit_book_path(book.id) %></td>
                <td><%= link_to "destroy",book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></td>
            </tr>
            <% end %>
        </table>
        <h2>New book</h2>
        <%= form_for(@book) do |f| %>
            <% if :title == "" && :body == "" %>
            <% puts '2 errors prohibited this book from being saved:' %>
            <% elsif :title == "" %>
            <% puts '1 error prohibited this book from being saved:'%>
            <% elsif :body == "" %>
            <% puts '1 error prohibited this book from being saved:'%>
            <% end %>
            <p>Title</p>
            <%= f.text_field :title %>
            <p>Body</p>
            <%= f.text_area :body %>
            <p><%= f.submit '投稿' %></p>
        <% end %>
    </body>
</html>